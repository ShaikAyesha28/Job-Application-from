# Job-Application-from
This project is built using HTML and CSS only.
I used HTML to structure the page and CSS to style and position the image.
The main focus was on image alignment, sizing, and maintaining aspect ratio using CSS.
